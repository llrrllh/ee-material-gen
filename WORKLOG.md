# WORKLOG

## 2026-03-27

### 本次完成
- 修复预览区显示问题：横版页面两侧显示不全、长页面溢出、缩放与滚动联动异常。
- 优化右侧布局：预览区与代码编辑器支持拖拽分栏，支持比例持久化。
- 增强预览能力：自动按宽度适配、缩放控制、窗口/分栏变化后自动重适配。
- 历史记录支持右键删除（前端 + `DELETE /api/history/{history_id}`）。
- 历史记录策略改为手动保存：
  - 生成/精修默认不自动写入历史。
  - 新增顶部“保存到历史”按钮。
  - 新增接口 `POST /api/history` 手动写入历史。
- 扩展“最终精修”复选项，增加更多常用优化项（排版、可读性、转化、无障碍等）。

### 关键代码位置
- 前端主页面与交互：`static/index.html`
- 历史记录与生成逻辑：`main.py`
- API 回归测试：`tests/test_main_api.py`

### 验证结果
- 前端脚本语法检查通过（`node --check`）。
- 后端单元测试通过：`20/20`（`python -m unittest discover`）。
- 服务已可启动并正常响应基础接口（如 `/api/material-types`）。

### 当前状态
- 项目处于可用状态，核心链路（生成、精修、预览、历史、导出）可正常使用。
- 如后续出现个别异常页面，可基于具体 `history_id` 做定向修复。
